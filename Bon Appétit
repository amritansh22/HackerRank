void bonAppetit(vector<int> bill, int k, int b) 
{
    int i,sum=0;
    for(i=0;i<bill.size();i++)
    {
        if(i!=k)
        {
            sum = sum+bill[i];
        }
    }
    if(sum/2==b)
    {
        cout<<"Bon Appetit";
    }
    else
    {
        cout<<b-sum/2;
    }
}
